{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "{{MODULE_NAME}}.signoff.v1",
  "title": "{{MODULE_DISPLAY_NAME}} Sign-off Interaction",
  "type": "object",
  "description": "Placeholder interaction schema for the {{MODULE_NAME}} human node. Replace with your real contract.",
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["approved", "rejected"]
    },
    "rationale": {
      "type": "string"
    }
  },
  "required": ["decision"],
  "additionalProperties": true
}
