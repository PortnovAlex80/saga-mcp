{
  "description": "Contract test fixture index for the Module Authoring Kit. Each negative case isolates exactly one canonical validation failure mode. The conformance test asserts the validator reports each case's expectedErrorCodes. Add a new case by dropping a JSON file in negative/ and listing it here.",
  "valid": [
    { "id": "valid-golden", "path": "valid-golden/manifest.json", "description": "A canonical ProcessModuleManifest that passes validation." }
  ],
  "negative": [
    { "id": "missing-manifest-format-version", "path": "negative/missing-manifest-format-version.json", "expectedErrorCodes": ["MANIFEST_FORMAT_VERSION_EMPTY"] },
    { "id": "definition-missing", "path": "negative/definition-missing.json", "expectedErrorCodes": ["MANIFEST_DEFINITION_MISSING"] },
    { "id": "definition-field-array", "path": "negative/definition-field-array.json", "expectedErrorCodes": ["MANIFEST_DEFINITION_FIELD_INVALID"] },
    { "id": "resource-kind-unknown", "path": "negative/resource-kind-unknown.json", "expectedErrorCodes": ["RESOURCE_KIND_UNKNOWN"] },
    { "id": "resource-logical-id-duplicate", "path": "negative/resource-logical-id-duplicate.json", "expectedErrorCodes": ["RESOURCE_LOGICAL_ID_DUPLICATE"] },
    { "id": "handler-refs-duplicate", "path": "negative/handler-refs-duplicate.json", "expectedErrorCodes": ["HANDLER_LOGICAL_ID_DUPLICATE"] },
    { "id": "contract-ref-invalid", "path": "negative/contract-ref-invalid.json", "expectedErrorCodes": ["MANIFEST_CONTRACT_REF_INVALID"] },
    { "id": "compat-range-invalid", "path": "negative/compat-range-invalid.json", "expectedErrorCodes": ["MANIFEST_COMPAT_RANGE_INVALID"] }
  ]
}
