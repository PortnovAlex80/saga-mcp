{
  "description": "Negative case: outputContractRef is missing (not a plain object). Expected error code MANIFEST_CONTRACT_REF_INVALID at $.outputContractRef.",
  "expectedErrorCodes": ["MANIFEST_CONTRACT_REF_INVALID"],
  "manifest": {
    "manifestFormatVersion": "1",
    "definition": {
      "identity": { "name": "neg-ocr", "version": "0.1.0", "kind": "neg", "displayName": "Neg", "description": "negative" },
      "inputContract": { "id": "neg-ocr.input.v1" },
      "outputContract": { "id": "neg-ocr.output.v1" },
      "outcomes": [{ "code": "done", "description": "x", "terminal": true }],
      "flow": {
        "id": "neg-ocr.standard",
        "version": "0.1.0",
        "entryNodeId": "n",
        "nodes": [{ "id": "n", "label": "N", "kind": "lm", "description": "x", "inputSchema": { "id": "neg-ocr.input.v1" }, "outputSchema": { "id": "neg-ocr.output.v1" }, "emitsOutcome": "done" }],
        "transitions": [],
        "terminalNodeIds": ["n"]
      },
      "artifacts": [{ "type": "neg-ocr.out", "schema": { "id": "neg-ocr.output.v1" }, "authority": "worker", "description": "x" }],
      "policies": [],
      "invariants": [],
      "executionProfiles": []
    },
    "resourceIndex": [],
    "handlerRefs": [],
    "inputContractRef": { "schemaId": "neg-ocr.input.v1", "version": "1.0.0", "digest": "pending@wave-2" },
    "runtimeCompatibilityRange": "^3.0.0"
  }
}
