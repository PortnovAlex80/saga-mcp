{
  "description": "Negative case: resourceIndex entry has an unknown kind. Expected error code RESOURCE_KIND_UNKNOWN at $.resourceIndex[0].kind.",
  "expectedErrorCodes": ["RESOURCE_KIND_UNKNOWN"],
  "manifest": {
    "manifestFormatVersion": "1",
    "definition": {
      "identity": { "name": "neg-rk", "version": "0.1.0", "kind": "neg", "displayName": "Neg", "description": "negative" },
      "inputContract": { "id": "neg-rk.input.v1" },
      "outputContract": { "id": "neg-rk.output.v1" },
      "outcomes": [{ "code": "done", "description": "x", "terminal": true }],
      "flow": {
        "id": "neg-rk.standard",
        "version": "0.1.0",
        "entryNodeId": "n",
        "nodes": [{ "id": "n", "label": "N", "kind": "lm", "description": "x", "inputSchema": { "id": "neg-rk.input.v1" }, "outputSchema": { "id": "neg-rk.output.v1" }, "emitsOutcome": "done" }],
        "transitions": [],
        "terminalNodeIds": ["n"]
      },
      "artifacts": [{ "type": "neg-rk.out", "schema": { "id": "neg-rk.output.v1" }, "authority": "worker", "description": "x" }],
      "policies": [],
      "invariants": [],
      "executionProfiles": []
    },
    "resourceIndex": [
      { "logicalId": "bad", "path": "resources/bad.md", "kind": "not-a-real-kind", "digest": "pending@wave-2" }
    ],
    "handlerRefs": [],
    "inputContractRef": { "schemaId": "neg-rk.input.v1", "version": "1.0.0", "digest": "pending@wave-2" },
    "outputContractRef": { "schemaId": "neg-rk.output.v1", "version": "1.0.0", "digest": "pending@wave-2" },
    "runtimeCompatibilityRange": "^3.0.0"
  }
}
