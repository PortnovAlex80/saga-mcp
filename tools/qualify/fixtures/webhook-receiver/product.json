{
  "kind": "ek-qualify-product-fixture.v1",
  "product": "webhook-receiver",
  "description": "Qualification product fixture (webhook-receiver) - deterministic, zero runtime dependencies, verified hermetically on loopback/stdin only.",
  "inputs": [
    "src/server.mjs"
  ]
}
