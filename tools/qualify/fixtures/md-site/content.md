# md-site sample

The **deterministic** documentation site generator.

## Features

- headings, lists and paragraphs
- `inline code` and [links](https://example.com)

## Example

```js
const site = renderSite(markdown);
```

Built by the qualification corpus.
