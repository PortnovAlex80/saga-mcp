{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "saga3-ext.marketing.work-intent.v1",
  "title": "MarketingWorkIntent",
  "description": "WorkIntent contract bound to the marketing-author execution profile of lm-marketing@1.0.0. The Runtime stamps one WorkIntent per LM-node execution; the package declares the schema shape so the kernel can verify the binding. Wave 10 extensibility proof: package-local work-intent schema.",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "briefRef"],
  "properties": {
    "kind": {
      "type": "string",
      "const": "marketing.campaign-draft",
      "description": "Discriminator matching the execution profile's workIntentKind."
    },
    "briefRef": {
      "type": "string",
      "minLength": 1,
      "description": "Stable reference to the MarketingBrief this intent binds."
    }
  }
}
