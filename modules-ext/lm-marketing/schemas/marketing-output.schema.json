{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "saga3-ext.marketing.output.v1",
  "title": "CampaignDraft",
  "description": "Output contract for the lm-marketing@1.0.0 Process Module. The typed CampaignDraft envelope produced by the draft-campaign LM node. Wave 10 extensibility proof: a package-local output contract, authority 'worker', registered behind this opaque id.",
  "type": "object",
  "additionalProperties": false,
  "required": ["subject", "audience", "goal", "channels", "keyMessage", "body", "channelPlan", "callToAction"],
  "properties": {
    "subject": { "type": "string", "minLength": 1, "description": "Headline / subject line of the draft." },
    "audience": { "type": "string", "minLength": 1, "description": "Audience, sourced from the brief." },
    "goal": { "type": "string", "minLength": 1, "description": "Goal, sourced from the brief." },
    "channels": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "description": "Channels selected; every entry must appear in the brief's allowed set."
    },
    "keyMessage": { "type": "string", "minLength": 1, "description": "Key message, reproduced verbatim from the brief." },
    "body": { "type": "string", "minLength": 1, "description": "Draft body copy." },
    "channelPlan": { "type": "string", "minLength": 1, "description": "Per-channel placement plan." },
    "callToAction": { "type": "string", "minLength": 1, "description": "Call to action." }
  }
}
