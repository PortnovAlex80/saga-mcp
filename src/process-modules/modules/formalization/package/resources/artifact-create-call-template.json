{
  "_PRE_CALL_INSTRUCTION": "BEFORE calling artifact_create, you MUST Write the file to disk at <workspace_root>/<path>. artifact_create auto-computes content_hash from the file. Without the file, content_hash is NULL and the Formalization pipeline WILL fail.",
  "tool": "artifact_create",
  "arguments": {
    "project_id": "FILL_INTEGER_MACHINE_BOUND_PROJECT_ID",
    "epic_id": "FILL_INTEGER_MACHINE_BOUND_EPIC_ID",
    "project_repository_id": "FILL_INTEGER_MACHINE_BOUND_PROJECT_REPOSITORY_ID",
    "type": "FILL_NODE_OWNED_ARTIFACT_TYPE",
    "title": "FILL_ARTIFACT_TITLE",
    "path": "FILL_RELATIVE_ARTIFACT_PATH_docs_requirements_REQ_NNN_slug",
    "code": "FILL_STABLE_CODE_OR_NULL",
    "status": "draft",
    "parent_artifact_id": "FILL_INTEGER_PARENT_ID_OR_NULL",
    "metadata": {
      "process_module_ref": "solution-formalization@1.0.0",
      "process_run_id": "FILL_MACHINE_BOUND_PROCESS_RUN_ID",
      "node_id": "FILL_MACHINE_BOUND_NODE_ID",
      "work_intent_id": "FILL_INTEGER_MACHINE_BOUND_WORK_INTENT_ID",
      "task_id": "FILL_INTEGER_MACHINE_BOUND_TASK_ID",
      "execution_id": "FILL_MACHINE_BOUND_EXECUTION_ID",
      "input_snapshot_hash": "FILL_MACHINE_BOUND_INPUT_HASH"
    }
  },
  "_TYPE_SPECIFIC_NOTES": {
    "brief": "When type='brief', metadata.brief_payload MUST be an object with: { decision: 'go'|'fast-track'|'clarify'|'reject', reasoning: 'non-empty string', affected_projects: [...], topology_hint: 'sequence'|'parallel-independent'|..., completeness: 'high'|'medium'|'low', ... }. Without brief_payload, artifact_create is REJECTED.",
    "PRD": "PRD artifacts require a derived_from → brief trace after creation.",
    "FR": "FR artifacts require a derived_from → PRD trace after creation.",
    "NFR": "NFR artifacts require a derived_from → PRD trace after creation.",
    "RULE": "RULE artifacts require a derived_from → PRD trace after creation.",
    "UC": "UC artifacts require a covers → FR trace after creation.",
    "AC": "AC artifacts require a derived_from → FR and/or derived_from → UC trace after creation.",
    "SRS": "SRS artifacts require a derived_from → PRD trace. The SRS must contain §12 Decision Log and §D2 stanzas."
  },
  "_POST_CALL_VERIFY": "After artifact_create, call artifact_list and verify content_hash is NOT null. If null, the file was not found — Write it and call artifact_create again."
}
