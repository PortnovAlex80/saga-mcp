/**
 * workflow-kernel/workshops/synthetic/index.ts - the WP-11V public
 * surface: the SYNTHETIC NON-GAME WORKSHOP (a report generator) installed
 * as pure manifest + mapping + CheckPlan data over the SAME frozen kernel
 * - the EK-8 generalization proof (plan EK-8: "Add a synthetic non-game
 * workshop and prove it requires no new kernel transition kind, table,
 * driver or reconciler").
 *
 * The package imports NO other workshop package: its scenario runs over
 * the kernel composition alone (obligation consumer, sole-writer
 * repositories, admitting transport, capsule ingress, staged vertical).
 */

export * from './installation.js';
export * from './products.js';
export * from './bindings.js';
export * from './scenario.js';
