{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "synthetic.compliance.input.v1",
  "title": "Synthetic Compliance Check Input (W2-A7 placeholder)",
  "type": "object",
  "description": "Opaque placeholder schema. Wave 1's ContractSchemaRegistry (or a Wave 2+ caller) registers a real codec behind this id; this fixture carries only the schema id.",
  "additionalProperties": true,
  "properties": {
    "workspaceRef": {
      "type": "string",
      "description": "Opaque reference to the workspace under compliance check."
    }
  },
  "required": ["workspaceRef"]
}
