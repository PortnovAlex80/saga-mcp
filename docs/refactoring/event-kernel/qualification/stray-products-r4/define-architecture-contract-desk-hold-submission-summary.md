# define-architecture-contract desk (author) — UPSTREAM HOLD submission summary

**Emission:** UH-Define-Architecture-Contract-001 (stray-products-r4, author seat)
**artifact:** `sha256:b831c67ed75bfc56024ddd78407a8ef8fdec593e6998963d86905b30c4bfb33b` (`define-architecture-contract-desk-upstream-hold.artifact.json`)
**trace:** `sha256:e5a4749ec21bfaff7042c421fa832e64820ce5ef61f271ecf2801afe343656f9` (`define-architecture-contract-desk-upstream-hold-trace.json`)
**verification:** `sha256:67a42de4dc800d68a9bbf8e5c9596bd4a4ea96040482e758ed2322ee49b44eed` (`define-architecture-contract-desk-hold-verify-out.json`, 36/36 recomputations pass)
**decision:** `hold-no-authoring` — no architecture-contract / SRS material authored; no scenario, surface, realization entry, obligation or digest pin fabricated.

## Why this desk holds instead of authoring

The desk's **sole lawful upstream input** is the frozen whole-WHAT baseline (`frf-contracts.what-baseline.v1`), delivered by `freeze-what-baseline` on `domain.frozen`, which folds `chain.baseline` — and **it does not exist**. The freeze desk refused across every staffing: r3 hold `UH-Freeze-What-Baseline-001` (`9f2d28b9…`), r3 reviewer stage `RC-Freeze-What-Baseline-001` (`c19344fd…`, hold-upheld), r3 author re-staff `AS-Freeze-What-Baseline-001` (`c2a08f04…`, standing hold), and the r4 adjudication `FR-Freeze-What-Baseline-Reviewer-002` (`d52746b6…`, hold-upheld — it hash-resolved that frame's "upstream-accepted `e210334e…`" claim to the contract-suite **green fixture** (whole-JSON canonical recomputed in this build) and REFUSED it as acceptance authority). The chain census of record stands at **0 of 5 pre-freeze desks accepted** (every verdict-of-record digest and verdict recomputed fresh in this emission), and the reconcile-what **no-accept prohibition** (`39a94a29…`, restated repair `702fc967…`) is undischarged.

The installed gate is fail-closed **before any product validation**: `dispatch.mjs` case `define-architecture-contract` requires `chain.baseline` — *"the SRS realizes the frozen whole-WHAT baseline and the accepted UC set"* — and refuses `MISSING_LINEAGE`. The accepted id-set universe (frozen `ucScenarioIds`, frozen `evidenceBindingIds`, `whatBaselineDigest`) plus the accepted `srsRevisionDigest` pin **do not exist** on this chain. Authoring the SRS anyway would mean fabricating the frozen universe — realizing scenarios never frozen, citing evidence bindings never accepted, pinning a baseline never sealed — the exact content-states-that-do-not-exist family this corpus has adjudicated CRIT since r1.

## What this staffing additionally records

- **This desk's own r1 stray authoring.** `AC-Define-Architecture-Contract-001` (+ updated variant) was authored against **zero accepted upstream revisions** (its own summary says so), approved with **zero findings** by verdicts carrying no verification — one of which passed *"0 accepted upstream revisions travel by content address"* **as a PASS criterion** — over a claim projection **disjoint** from the accepted capsule (0/7 overlap with the 8 envelope addresses), at claimed digests `a926df62…` / `8b2ec93c…` that recompute from **no file in the workspace** (the declaring r1 files recompute to `e2ae5d31…` / `1f06d663…`). Recorded as superseded stray history; NOT product lineage.
- **Frame pins are the r1 digest-drift family.** This frame's **protocol-skill** pin `a926df62…` is the r1 CRIT-003 governing anchor (unresolvable since r2's MAJ-1); its **semantic-skill** pin `95fafc84…` is the r1 acceptance-contract artifact's self-claimed digest (recomputes to `f53af964…`; the r3 verifiers already labeled it `STALE_R1_PROTOCOL_SKILL`). A fresh workspace-wide three-layer scan (raw bytes, whole-JSON canonical, `.content` canonical; 2,770 files; `.git`/`node_modules`/this emission excluded) hash-resolves **both — and `8b2ec93c…` — to zero contents**. Recorded verbatim as envelope provenance; **not ratified**; the hold does not depend on either. The installed desk's own declared semantic-skill digest recomputes to `131efbd9…` and honestly differs from the frame pin.
- **Only accepted base unchanged:** the discovery import chain (`b10bb762…`, capsule 9/9 recomputed, envelope 8/8 matched, CERT-1 `03972527…`). `terminal:audited-1` and `terminal:delivered-1` are carried as verified capsule addresses only — **neither is asserted satisfied**.

## Verification

`define-architecture-contract-desk-hold-verify.mjs` — **36/36 checks pass** (verify-out digest-pinned above): emission self-digests + acyclic trace (V1), hold semantics + no-authoring fence (V2), accepted-base re-derivation (V3), census 0/5 with every cited digest and verdict recomputed (V4), freeze refusal chain + impostor fixture resolution (V5), gate law grounded in installed source (`dispatch.mjs` / `srs-realization/desk.ts` / `manifest.ts`) (V6), desk-declared vs frame semantic pin (V7), r1 stray history + projection disjointness (V8), the workspace-wide frame-pin scan (V9), file-family discipline + determinism probes (V10). Artifact and trace re-derive byte-stable from `define-architecture-contract-desk-hold-build.mjs`.

One emission-repair landed this staffing (kept out of the digest-pinned records): the verifier's fence check initially tripped over the fence block's own key list and its file-family check ran before verify-out/summary were written; both checks were corrected to scan content-minus-fence-declaration and to assert subset-plus-no-unexpected-file respectively. The hold artifact and trace bytes never changed (`b831c67e…` / `e5a4749e…` re-derive from the builder).

## Resume contract (R1–R5)

R1: genuinely accepted revisions land for the five pre-freeze desks through completed reviewer stages at their own content addresses, with the `CL-Define-Acceptance-Contract-001` contention adjudicated by driver/human first. R2: the re-run reconcile-what reviewer verdict of record alone discharges the no-accept prohibition — never this desk, never a frame assertion. R3: freeze-what-baseline re-staffs and authors the whole-WHAT baseline strictly against the accepted triples and `frf-contracts.what-baseline.v1`; only then does `domain.frozen` fire and `chain.baseline` exist. R4: on the frozen baseline at its own content address, this desk re-staffs and authors the architecture contract strictly against the frozen universe (frozen scenario ids, frozen evidence-binding ids, the frozen `whatBaselineDigest`) plus an accepted `srsRevisionDigest` pin, through the installed `formalization.srs-structure.v1` / `validateSrs` surface. R5: this hold is not carried as product lineage; the SRS cites only the frozen baseline and accepted revisions; the r1 stray authoring stays superseded history.
