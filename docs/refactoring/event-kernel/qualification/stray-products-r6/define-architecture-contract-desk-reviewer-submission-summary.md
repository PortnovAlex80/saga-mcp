# define-architecture-contract desk (reviewer) - refusal emission (stray-products-r6)

Emission of record: **FR-Define-Architecture-Contract-001** (verdict `hold-upheld`;
frame upstream-accepted claim REFUSED; no product verdict minted; `effectFired: false`).

- Review: sha256:d813908b481afeba8466fc1ad6734338b59df766da7c08ed3cb8d12f08798511
- Verification: sha256:63c0ff37ac8eca0c15e747e625fd0ceb77d6e03e602a1b64e67d138b36e086a1
- Trace: sha256:e7965ac30699c394e66936dc3bbf3a6db28fedc7a805edd87f8e68e6d6f3d3a3
- Submission: sha256:600a6d4fa2cac4558a446b27194ea68cc95ecb3ea1cc6f5ff15336178596dfda
- Independent verifier: 53/53 green (`-verify-out.json`, self-digested `7ea21bfd...`)

## Why the reviewer seat holds and refuses

The reviewed candidate of record is the r5 author-seat hold **UH-Define-Architecture-Contract-001**
(`6a32f180...`, `hold-no-authoring`, `noProductAuthored: true`, census 0 of 6, verifier 29/29):
it re-derives byte-stable and is **upheld**. NO SRS candidate exists at this desk - none was
ever lawfully authorable - so the accept/repair verdict vocabulary stays uncomputed over the
absent subject, exactly as the desk's first reviewer-stage record (the testbed
**UH-Define-Architecture-Contract-002**, `83501c22...`, hold-no-review) required. This package
is minted because NEW adjudication content exists since that record: the r5 author-hold
re-emission and this staffing's workspace-wide re-scan.

## The frame's authority claim: REFUSED (phantom-upstream-projection)

The frame projects `upstream-accepted[0] sha256:b7f34c48... :: "accepted revision of
define-architecture-contract"` with the line *"workspace: 1 accepted upstream revisions travel
by content address"* - adjudicated **FALSE** on three independent grounds:

1. **Content-unresolved** - a workspace-wide three-body scan (raw bytes, whole-JSON canonical,
   `.content` canonical; ~2800 files) hash-resolves the address to ZERO contents.
2. **Process-impossible** - the desk of record authors no product; no author candidate,
   submission, intake receipt, gate verdict or reviewer verdict exists anywhere for this desk.
3. **Wrong-referent** - the entry names THIS desk's own product kind (`formalization.srs.v1`);
   the lawful upstream supply is the frozen WHAT baseline + the accepted `srsRevisionDigest`
   pin over the never-fired `domain.frozen` edge (freeze ratification REFUSED by
   FR-Freeze-What-Baseline-002 `d52746b6...`, effect never fired).

Every textual mention of the address lives in refusing adjudication/verification records (the
testbed reviewer-hold set; the settle-formalization hold verifiers, which name it the
"arch-own-revision" phantom). Zero ratifying citations.

## Verified alongside (all recomputed, nothing trusted)

- Envelope 8/8 re-derives from the accepted discovery-import capsule (9/9 with CERT-1) - still
  the only accepted base; census 0 of 6 upstream desks.
- Frame skill pins (`bc8a4261...`/`2cbcf850...`) resolve to zero contents; installed manifest
  pins recompute (`b88267a1...`/`131efbd9...`) and differ - provenance, never ratified. The
  inherited governing anchor `a926df6284...` remains unresolvable.
- Parallel testbed round recomputes end-to-end (author hold `d58e6a6a...`; reviewer hold
  `83501c22...`; receipts VERIFIED).
- Round co-tenancy: the settle-formalization desk emitted its own holds concurrently into this
  shared round directory (r6 co-tenant `b40d7616...`, testbed twin `8e1bcf73...`, both
  `hold-no-authoring`, verifiers green) - the post-freeze spine is holding consistently.
- Live-movement observation: derived/regenerable artifacts were rewritten in the workspace
  during this staffing; every immutable record body this review binds recomputed byte-stable,
  and no accepted lineage appeared.

## Resume contract

R1-R5 of the r5 author hold unchanged: the freeze desk R1-R4 completes first (five genuinely
accepted pre-freeze desks -> RA-5 reconcile-what re-run -> its reviewer verdict alone discharges
the no-accept prohibition -> freeze ratifies); this desk re-staffs only against the REAL frozen
WHAT-baseline revision; the author stage authors and admits a candidate by intake receipt; only
then does this reviewer seat review over the admitted candidate. The holds and this refusal are
never carried as product lineage; the r1 stray product stays retired.
