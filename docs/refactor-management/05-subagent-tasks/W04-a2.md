# W4-A2 ProtocolRuntime state machine

**Wave:** 4 · **Lane:** a2 · **Spec:** `09-contracts/WAVE4-PROTOCOL-RECOVERY-SPEC.md`
**Frozen input:** `a46d3ea` · **Branch:** `refactor/w4-a2` · **Worktree:** `.worktrees/w4-a2`

## Read first
1. This task file. 2. `09-contracts/WAVE4-PROTOCOL-RECOVERY-SPEC.md` (full). 3. Recon: `domain/spi/node-protocol.ts`, `domain/spi/recovery-definitions.ts`, `domain/recovery.ts`, `persistence/recovery-case.ts` + `sqlite-recovery-case-repository.ts` (existing recovery system). 4. Wave 1 SPI barrel `domain/spi/index.ts`.

## Own
Own: application/protocol-runtime.ts (NEW). Pure transition state machine: startStep, checkEvidence, completeStep, retryStep, pauseProtocol, resumeProtocol, handleRecovery. Uses NodeProtocolDefinition (Wave 1) + ProtocolRunRepository (A1). No module semantics. Validates transitions (linear/branch/repeat) against the protocol definition. Reject illegal transitions.

## Verify
`npm run build && node --test <your-test-file> && node --test tests/architecture/dependency-direction.test.mjs`

## Commit
`feat(protocol): W4-A2 ProtocolRuntime state machine`

## Return
Branch+sha, diff --stat, test tail+ratchet, exported symbols, confirmation. Escalate ambiguities (plan §0.1.7).
