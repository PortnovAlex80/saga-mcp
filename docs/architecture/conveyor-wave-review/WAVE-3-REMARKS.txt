ВОЛНА 3. РАБОЧИЙ ПОЛУЧАЕТ РАБОТУ И НЕ ДИСПЕТЧЕРИЗУЕТ

Статус проверки: выполнено частично.

Основные замечания:

1. Новые launch-пути используют AssignedWork, но старый worker_next и связанный claimScope ещё сохраняются как compatibility path. Пока назначенный рабочий способен снова обратиться к выдаче работы, принцип одного запуска на одну карточку не обеспечен.

2. Ограничение worker_next должно действовать на уровне capability/tool authorization. Удаление текста из skill или инструкции не является защитой.

3. Нужно проверить все launcher implementations, включая Claude runner, legacy adapters, тестовые runner paths и запуск из process modules. Один старый launcher с taskId вместо AssignedWork сохраняет обход архитектуры.

4. Не доказано, что worker_done завершает текущую execution и после этого процесс не получает следующую карточку тем же запуском.

5. Compatibility adapter должен находиться только на infrastructure boundary, иметь telemetry и конкретное условие удаления. Наличие claimScope без счётчика использования не позволяет доказать нулевое применение.

Исследовать и исправить:

Найти все упоминания worker_next, claimScope, readClaimableTasks и старых launcher signatures. Для execution с AssignedWork запретить worker_next в авторизаторе инструментов. Удалить worker_next из capability packages и skills Development и Formalization. Оставшийся compatibility adapter снабдить audit event и метрикой, затем доказать ноль вызовов в end-to-end тестах и удалить адаптер.

Критерий завершения:

Каждый production worker стартует с точными card, workplace, desk, execution и fence. Вызов worker_next из назначенной execution возвращает запрещённую операцию ещё до обращения к очереди. Один launch обрабатывает ровно одну карточку, вызывает worker_done и заканчивается. End-to-end тесты показывают ноль compatibility-path calls.

ПОВТОРНАЯ ПРОВЕРКА 2026-08-02

Новый статус: почти выполнено, но формальный exit criterion ещё не закрыт.

Что изменилось:

WorkerExecutorStart.assignment теперь обязательный. В исполняемом src отсутствует claimScope. ClaudeBoardRunner запускает одну заранее назначенную карточку и добавляет mcp__saga__worker_next в disallowedTools. Архитектурный тест no-claim-scope проходит. Основные skills Development и Formalization прямо запрещают worker_next.

Что остаётся:

Capability package platform.worker-completion всё ещё включает worker_next. В общих skills saga-code-reviewer, saga-readiness-checker, saga-perf-tuner, saga-type-fixer, saga-tracker и saga-dispatch по-прежнему описан self-claim через worker_next. Это может быть отдельный ручной MCP режим, но документ требует удалить worker_next из worker allowed tools и capability packages. Запрет через аргумент конкретного Claude launcher не доказывает запрет для любого другого launcher или MCP клиента с execution fence.

Исследовать и исправить:

Разделить dispatcher capability и assigned-worker capability. Удалить worker_next из пакета, выдаваемого назначенной execution. Добавить server-side authorization test: запрос worker_next с действующим assigned executionId или fence отклоняется до чтения очереди независимо от клиента и launcher. Классифицировать оставшиеся self-claim skills как legacy/manual dispatcher surface либо переписать их. Доказать ноль compatibility вызовов end-to-end.

ТРЕТЬЯ ПРОВЕРКА 2026-08-02

Новый статус: Wave 3 можно принять.

worker_next удалён из capability package назначенного рабочего. Server-side handler отклоняет worker_next, если execution активна или владеет task fence. Контрольные тесты подтверждают, что dispatcher без assigned fence продолжает назначать работу. ClaimScope отсутствует, runner получает обязательный AssignedWork и запускает одну карточку.

Исследовать и исправить:

Wave 3 закрыть. Сохранить server-side fence rejection главным инвариантом, потому что client-side disallowedTools является только дополнительной защитой.

ЧЕТВЁРТАЯ ПРОВЕРКА 2026-08-02

Статус не изменился: Wave 3 принята. Server-side worker_next rejection и no-claim-scope gate проходят повторно.

Исследовать и исправить:

Новых обязательных исправлений нет. Не заменять server-side authority check одним client-side disallowedTools.
