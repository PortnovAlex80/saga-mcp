ВОЛНА 1. ДОМЕННЫЕ КОНТРАКТЫ И ЕДИНЫЙ ЯЗЫК

Статус проверки: выполнено частично.

Основные замечания:

1. В кодовой базе появились новые нейтральные контракты и порты, но старые понятия task, worker task, execution и card продолжают использоваться одновременно. Граница между карточкой работы и одноразовой попыткой рабочего выражена не везде одинаково.

2. AssignedWork ещё не доказан как единственный вход запуска рабочего. Сохраняются старые сигнатуры, совместимые объекты и адаптеры, через которые запуск может получить projectId, taskId или execution metadata раздельно.

3. Наличие интерфейсов WorkplaceRef, CardRef, ExecutionId, FenceToken, Lease, DeskRef и ProductRef само по себе не доказывает применение доменных инвариантов. Нужно проверить все места создания этих значений и запретить несовместимые ссылки, а не только описать типы TypeScript.

4. Часть архитектурных проверок проверяет исходный текст и имена импортов. Такие тесты можно пройти, не используя новые контракты в production path.

5. Не доказано единым набором unit-тестов, что неверные переходы состояния, чужая карточка, чужое рабочее место, устаревший fence и несовместимый executionId всегда отвергаются.

Исследовать и исправить:

Найти все публичные команды, порты и launch API, где рабочая карточка называется task или где worker запускается без полного AssignedWork. Составить список оставшихся compatibility API и их production consumers. Перевести production launch path на один AssignedWork. Добавить runtime-конструкторы или валидаторы для ссылок и тесты отрицательных сценариев. Запретить новые публичные worker-task контракты архитектурным тестом по TypeScript AST или импортному графу, а не поиском комментариев.

Критерий завершения:

Все production launchers принимают AssignedWork. Карточка и execution являются разными типами. Несовместимые workplace, card, desk, execution и fence отклоняются тестами. Domain-код не импортирует adapters, database или infrastructure. В публичных контрактах попытка рабочего нигде не называется task.

ПОВТОРНАЯ ПРОВЕРКА 2026-08-02

Новый статус: выполнено частично, есть существенный прогресс.

Что изменилось:

AssignedWork теперь обязателен в WorkerExecutorStart, а claimScope удалён из исполняемого кода. Architecture gate подтверждает наличие всех заявленных портов и value-object интерфейсов.

Что остаётся:

WorkplaceRef, CardRef, DeskRef, ProductRef и Lease в основном существуют как объявления в application/ports/conveyor-ports.ts и почти не используются production-кодом. ExecutionId брендирован отдельно в lifecycle/domain/ids.ts, но AssignedWork по-прежнему содержит обычные string и number. Отдельного брендированного FenceToken нет, workerExecutionId и fenceToken дублируются как две строки. Поэтому несовместимые ссылки всё ещё можно перепутать на границе компиляции и runtime.

Исследовать и исправить:

Перевести команды и порты production path на брендированные CardRef, ExecutionId, FenceToken и WorkplaceRef. Добавить конструкторы с runtime validation. Устранить дублирование workerExecutionId и fenceToken либо выразить их связь одним типом. Добавить отрицательные unit-тесты на смешение card, execution, workplace и fence. Не считать тест, который только находит имена интерфейсов в исходнике, доказательством применения доменной модели.

ТРЕТЬЯ ПРОВЕРКА 2026-08-02

Новый статус: Wave 1 можно принять по критериям текущего документа.

CardId и FenceToken стали отдельными branded types с runtime-validating конструкторами. AssignedWork использует CardId, ExecutionId и FenceToken. Production assignment boundary вызывает asCardId, asExecutionId и asFenceToken. Добавлены отрицательные rejection tests. AssignedWork обязателен для запуска, claimScope отсутствует.

Исследовать и исправить:

Wave 1 закрыть. Оставить type-level и runtime rejection tests обязательными. Дальнейшее расширение branded types на epicId и projectId является улучшением, но не блокирует текущие exit criteria.

ЧЕТВЁРТАЯ ПРОВЕРКА 2026-08-02

Статус не изменился: Wave 1 принята. Build и architecture gate проходят. Новых нарушений AssignedWork, CardId, ExecutionId и FenceToken не обнаружено.

Исследовать и исправить:

Новых обязательных исправлений нет. Сохранять rejection tests и запрет claimScope в CI.
