name: Apply Saga4 test contract sync

on:
  pull_request:
    branches:
      - saga4

permissions:
  contents: write
  issues: write

jobs:
  apply:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Waiting for verified patch payload"
