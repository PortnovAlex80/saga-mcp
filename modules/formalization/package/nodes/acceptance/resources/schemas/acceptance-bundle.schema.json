{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "saga3.formalization-acceptance-bundle.v1",
  "title": "Formalization Acceptance Bundle (W8-A4)",
  "description": "Output bundle of the define-acceptance-contract + resolve-acceptance-contract node pair. Carries the accepted AC artifact refs + the derived_from/covers trace refs the kernel resolver validated. Concrete codec registered by Wave 2/3 ContractSchemaRegistry behind this id.",
  "type": "object",
  "additionalProperties": true,
  "required": ["schemaVersion", "formalizationEpicId", "acArtifactIds"],
  "properties": {
    "schemaVersion": {
      "const": "saga3.formalization-acceptance-bundle.v1"
    },
    "formalizationEpicId": {
      "type": "integer",
      "minimum": 1
    },
    "acArtifactIds": {
      "type": "array",
      "items": { "type": "integer", "minimum": 1 }
    },
    "traceIds": {
      "type": "array",
      "items": { "type": "integer", "minimum": 1 }
    }
  }
}
