node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.claude/settings.json
# tracker-view runtime artifacts
.tracker-view.pid
tracker-view/.tracker-view.pid

# git worktrees (saga task isolation: .worktrees/task-<id>)
.worktrees/

# ZCode client session artifacts (local plans, not part of the repo)
.zcode/

# temp test/build logs
*.log

# worktree-local test scratch (TEMP/TMP for test runs; never committed)
.test-tmp/

# Wave 0 hygiene (W0-A8): leftover SQLite DBs + per-epic/orchestrate run logs
# should never be checked in. APPEND only — do not remove prior entries.
tests/planner-ac9/.tmp-*.db*
*.tmp.db*
epic*-run.log
orchestrate-*.log

# Runtime/test databases created by bootstrap scripts and manual testing
saga*.db*
.tracker.db*
saga4-*.db*

# Real/model factory smoke outputs (DB, logs, generated product repositories)
.real-factory-smoke/
.real-factory-smoke-*/

# Factory run artifacts — leftover from mock/real factory runs in the repo root.
# These are never source; the orchestrator and simulator regenerate them per run.
db.sqlite
db.sqlite-*
*.sqlite
*.sqlite-*
# WP-13C: the committed elite-evidence-kit determinism fixture (CI smoke source)
!tests/infrastructure/ek-fixtures/elite-smoke/factory.sqlite
saga-launch-ref.txt
sim-workspace/

# Generated product emitted into repo root by factory smoke runs.
# Root-anchored (2026-08-26): the bare pattern silently ate every nested
# fixture/product index.html (WP-08 simple-server, WP-15 qualify fixtures) —
# committed-but-vanishing files, red builds on every fresh checkout.
/index.html

# Saga runtime package-store — content-addressed cache of module resources,
# materialized at install time by production-install.ts. Regenerable from
# src/process-modules/<module>/package/resources/. Never check in.
.saga/package-store/

# Saga project binding — per-machine pointer to a tracker DB project row.
# Created by `saga start` for local development; not portable across machines.
.saga/project.json
.button-color-replay-e2e/
.factory-sandboxes/
.real-factory-smoke/
tests/golden-runs/
.factory-testbed/

# generated status-sheet artifacts
docs/testing/WORKSHOP-STATUS-SHEET.pdf
docs/testing/WORKSHOP-STATUS-SHEET.png
docs/refactoring/event-kernel/specs/.ek-admission-vector.json
*.pid
